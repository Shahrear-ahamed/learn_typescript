type GenericRollType<T> = Array<T>

const rollsNumber: Array<number> = [1, 2, 3]

// generic with
type GenericTuple<X, Y> = [X, Y]
const studentTuple: GenericTuple<string, number> = ["shahrear", 22]

